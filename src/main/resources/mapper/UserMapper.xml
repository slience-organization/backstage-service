<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.star.mapper.UserMapper">

    <sql id="uid"> select REPLACE(uuid(),'-',''); </sql>

    <!--    <select id="getAll" resultType="com.smart.mall.entity.BhUserEntity">-->
    <!--        select * from bh_user-->
    <!--    </select>-->

    <!--    <select id="getUserByPhone" resultType="com.smart.mall.entity.BhUserEntity">-->
    <!--        select id from bh_user where phone = "phone"-->
    <!--    </select>-->

    <select id="getUserById" resultType="com.smart.star.entity.CkUserEntity">
        select * from ck_user where id = #{id}
    </select>

    <select id="getUserByPhone" resultType="com.smart.star.entity.CkUserEntity">
        select * from ck_user where phone = #{phone}
    </select>

    <insert id="register">
        <selectKey keyProperty="uid" resultType="string" order="BEFORE">
            select replace(uuid(), '-', '') as id from dual
        </selectKey>
        insert into ck_user (id,phone,password)
        values (#{uid},#{phone},#{password})
    </insert>

</mapper>